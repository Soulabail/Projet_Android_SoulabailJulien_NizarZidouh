package com.easycocktail.smarteaz;
import android.widget.TextView;

import com.squareup.okhttp.OkHttpClient;

import org.json.JSONArray;


public class Request {
/*
    //public static String URL = "https://www.thecocktaildb.com/api/json/v1/1/random.php";
    //public static String[] Php = {"random.php", // cocktail aléatoire};
    OkHttpClient okHttpClient = new OkHttpClient();
    Request myGetRequest = new Request.Builder()
            .url("https://api.github.com/users/florent37")
            .build();

okHttpClient.newCall(myGetRequest).enqueue(new Callback() {
        @Override
        public void onFailure(Request request, IOException e) {

        }

        @Override
        public void onResponse(Response response) throws IOException {
            //le retour est effectué dans un thread différent
            final String text = response.body().string();
            final int statusCode = response.code();

            runOnUiThread(new Runnable() {
                @Override
                public void run() {
                    TextView.setText(text);
                }
            });
        }
    });*/
}